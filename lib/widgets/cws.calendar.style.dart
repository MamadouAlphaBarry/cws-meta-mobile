import 'package:flutter/material.dart';

class CWSCalendarStyle extends StatelessWidget {
  const CWSCalendarStyle({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
