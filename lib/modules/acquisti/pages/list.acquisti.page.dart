import 'package:flutter/material.dart';

class ListAcquisti extends StatelessWidget {
  const ListAcquisti({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Card(
          child: Text("ListAcquisti"),
        ),
      ),
    );
  }
}
